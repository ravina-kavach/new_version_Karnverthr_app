import React from 'react'
import {CommonView, H4} from '../../utils/common'
import { ScrollView } from 'react-native'

const Home = () => {
  return (
    <CommonView>
      <ScrollView>
        <H4>Home</H4>
      </ScrollView>
    </CommonView>
  )
}

export default Home